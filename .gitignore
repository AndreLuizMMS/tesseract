# binary compiled right here
/ts
# local state of whoever runs Tesseract
*.log
# Windows metadata that WSL attaches to downloaded files
*:Zone.Identifier
# the zip that came with the brand kit; what matters are the extracted files
style/files.zip
